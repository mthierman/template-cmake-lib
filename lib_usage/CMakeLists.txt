add_executable(${PROJECT_NAME}_usage)

target_sources(${PROJECT_NAME}_usage PRIVATE "usage.cxx")

target_link_libraries(
    ${PROJECT_NAME}_usage
    PRIVATE ${PROJECT_NAME}::${PROJECT_NAME}
            ${PROJECT_NAME}::config
    )
